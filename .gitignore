# .gitignore for Filmes-Favoritos
# Purpose: evitar commitar arquivos locais, binários, dependências e segredos.

############################
# OS / filesystem
############################
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

############################
# Node / frontend (React)
############################
# dependencies
/frontend/node_modules/

# build outputs
/frontend/build/

# caches / logs
/frontend/.cache/
/frontend/npm-debug.log*
/frontend/yarn-debug.log*
/frontend/yarn-error.log*

# keep lockfiles (package-lock.json, yarn.lock) in repo

############################
# Python / backend
############################
# Virtualenvs
/backend/venv/
/backend/.venv/
/backend/env/

# bytecode, caches
/backend/__pycache__/
/backend/*.py[cod]
/backend/*.pyo
/backend/*.pyd

# packaging
/backend/*.egg-info/
/backend/pip-wheel-metadata/

# test / tooling
/backend/.pytest_cache/
/backend/.mypy_cache/

# general Python patterns (extra safety for any Python files at repo root)
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
pip-wheel-metadata/
htmlcov/
.coverage
.coverage.*
.pytest_cache/

############################
# Environment / secrets
############################
# local env files (ignore secrets). Keep an example file named .env.example committed.
.env
.env.local
.env.*.local
!.env.example

############################
# IDEs / editors
############################
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

############################
# Logs, temp, misc
############################
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# build / distribution
dist/
build/
coverage/
*.egg
*.whl

# Node at repo root (safety)
node_modules/

# local virtualenvs
.venv/
venv/

# misc
.envrc
*.sqlite3

