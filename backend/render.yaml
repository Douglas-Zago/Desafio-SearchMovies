services:
  - type: web
    name: desafio-searchmovies-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py makemigrations
      python manage.py migrate
    startCommand: gunicorn core.wsgi:application
